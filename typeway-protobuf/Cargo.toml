[package]
name = "typeway-protobuf"
version = "0.1.0"
edition = "2021"

[dependencies]
typeway-grpc = { path = "../typeway-grpc" }
bytes = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints]
workspace = true
